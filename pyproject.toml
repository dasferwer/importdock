[project]
name = "importdock"
version = "0.1.0"
description = "Возобновляемый импорт CSV и XLSX с отчётом по строкам"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.30,<1", "psycopg[binary]>=3.2,<4", "boto3>=1.35,<2", "pika>=1.3,<2", "openpyxl>=3.1,<4", "python-multipart>=0.0.20,<1"]
[dependency-groups]
dev = ["pytest>=8,<10", "httpx>=0.27,<1", "ruff>=0.9,<1"]
[tool.ruff]
line-length = 100
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
